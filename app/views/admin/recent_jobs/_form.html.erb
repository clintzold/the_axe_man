<%= form_with(model: admin_recent_job) do |form| %>
  <% if admin_recent_job.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_recent_job.errors.count, "error") %> prohibited this admin_recent_job from being saved:</h2>

      <ul>
        <% admin_recent_job.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :location, style: "display: block" %>
    <%= form.text_field :location %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
